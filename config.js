// ─── Environment Configuration ────────────────────────────────────────────────
// For LOCAL development: leave window.KHATA_BACKEND as "" (falls back to localhost:5000)
// For PRODUCTION: set this to your Render backend URL, e.g.:
//   window.KHATA_BACKEND = "https://khata-backend.onrender.com";

window.KHATA_BACKEND = ""; // ← replace with your Render URL before deploying
